function mul(a, b) {
    return a * b;
}

function main() {
    // NOTE:
    let a = 95 / 4;
    let b = 3.1415926535;
    let c = mul(a + a, b);
    return (c + 4) * (a - 75);
}

/*

TODO:
- integer
  - hex
  - binary
- double
  - 1.0e-10
- string
  - escape chars
- if
- for
- &&
- ||
- ==
- !=
- unary operators
- class

*/
