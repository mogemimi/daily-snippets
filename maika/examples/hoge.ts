function mul(a: int, b: int) {
    return a * b;
}

function main() {
    // NOTE:
    let a = 95 / 4;
    let b = 3.1415926535;
    let c = mul(a + a, b);
    let d = (a) = c;
    return (c + 4) * (a - 75);
}

/*

TODO:
- integer
  - hex
  - binary
- if
- for
- unary operators
- class

*/
